title: DecoM
description: 'una tarea de diseño de pagina Web '
template:
  repo: decom-template
  name: decom
before:
  - type: createIssue
    title: Welcome!
    body: new-issue.md
steps:
  - title: First step
    description: The first step in your course
    event: issues.assigned
    link: '{{ repoUrl }}'
    actions:
      - type: closeIssue
